name: Build and Release Executable

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (optional)'
        required: false
        default: 'dev'

env:
  PYTHON_VERSION: '3.13'

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest pytest-cov panda3d pyinstaller

    - name: Run tests
      run: |
        python -m pytest --tb=short

    - name: Build executable
      shell: bash
      run: |
        python -m PyInstaller --onedir --windowed --name PrimevalAtom --collect-all panda3d --collect-all direct --runtime-hook=panda3d_runtime_hook.py --exclude-module=tkinter --exclude-module=unittest --exclude-module=tests --exclude-module=pytest --exclude-module=coverage --exclude-module=setuptools --clean main.py

    - name: Create distribution archive
      run: |
        # Create a zip file with the executable and any required files
        cd dist
        Compress-Archive -Path "PrimevalAtom" -DestinationPath "../PrimevalAtom-Windows.zip"
        cd ..

    - name: Upload workflow artifact
      uses: actions/upload-artifact@v4
      with:
        name: PrimevalAtom-Windows
        path: ./PrimevalAtom-Windows.zip

  build-ubuntu:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install system dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libgl1-mesa-glx libgl1-mesa-dri libxcb-xfixes0 libxcb-icccm4 libxcb-image0 libxcb-keysyms1 libxcb-randr0 libxcb-render-util0 libxcb-shape0

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest pytest-cov panda3d pyinstaller

    - name: Build executable
      run: |
        python -m PyInstaller --onedir --windowed --name PrimevalAtom --collect-all panda3d --collect-all direct --runtime-hook=panda3d_runtime_hook.py --exclude-module=tkinter --exclude-module=unittest --exclude-module=tests --exclude-module=pytest --exclude-module=coverage --exclude-module=setuptools --clean main.py

    - name: Create distribution archive
      run: |
        cd dist
        tar -czf ../PrimevalAtom-Linux.tar.gz PrimevalAtom
        cd ..

    - name: Upload workflow artifact
      uses: actions/upload-artifact@v4
      with:
        name: PrimevalAtom-Linux
        path: ./PrimevalAtom-Linux.tar.gz

  build-macos:
    runs-on: macos-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest pytest-cov panda3d pyinstaller

    - name: Build executable
      run: |
        python -m PyInstaller --onedir --windowed --name PrimevalAtom --collect-all panda3d --collect-all direct --runtime-hook=panda3d_runtime_hook.py --exclude-module=tkinter --exclude-module=unittest --exclude-module=tests --exclude-module=pytest --exclude-module=coverage --exclude-module=setuptools --clean main.py

    - name: Create distribution archive
      run: |
        cd dist
        tar -czf ../PrimevalAtom-macOS.tar.gz PrimevalAtom
        cd ..

    - name: Upload workflow artifact
      uses: actions/upload-artifact@v4
      with:
        name: PrimevalAtom-macOS
        path: ./PrimevalAtom-macOS.tar.gz

  upload-release-assets:
    if: github.event_name == 'release'
    needs: [build-windows, build-ubuntu, build-macos]
    runs-on: ubuntu-latest
    steps:
    - name: Download Windows artifact
      uses: actions/download-artifact@v4
      with:
        name: PrimevalAtom-Windows

    - name: Download Linux artifact
      uses: actions/download-artifact@v4
      with:
        name: PrimevalAtom-Linux

    - name: Download macOS artifact
      uses: actions/download-artifact@v4
      with:
        name: PrimevalAtom-macOS

    - name: Upload release assets
      uses: softprops/action-gh-release@v2
      with:
        files: |
          PrimevalAtom-Windows.zip
          PrimevalAtom-Linux.tar.gz
          PrimevalAtom-macOS.tar.gz
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}